/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        PATH_URL: "http://localhost:3001",
    },
    images: {
        domains: ['www.freeiconspng.com'],
    }
};

export default nextConfig;
